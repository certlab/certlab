// Legacy auth file - now using Replit Auth through lib/auth-provider.tsx
// Keeping this file for compatibility but redirecting to new auth system

export { useAuth, AuthProvider } from '@/lib/auth-provider';