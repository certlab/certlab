name: Dependabot Auto-Merge

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to process (leave empty to skip)'
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    # Only run on Dependabot PRs or manual workflow dispatch
    if: github.actor == 'dependabot[bot]' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Get PR details for manual trigger
        if: github.event_name == 'workflow_dispatch' && inputs.pr_number != ''
        id: pr-details
        run: |
          PR_NUMBER="${{ inputs.pr_number }}"
          PR_DATA=$(gh pr view "$PR_NUMBER" --json author,url --repo ${{ github.repository }})
          PR_AUTHOR=$(echo "$PR_DATA" | jq -r '.author.login')
          PR_URL=$(echo "$PR_DATA" | jq -r '.url')
          
          if [[ "$PR_AUTHOR" != "dependabot[bot]" ]]; then
            echo "Error: PR #$PR_NUMBER is not from Dependabot"
            exit 1
          fi
          
          echo "pr-url=$PR_URL" >> $GITHUB_OUTPUT
          echo "pr-number=$PR_NUMBER" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine if update should be auto-merged
        id: should-auto-merge
        run: |
          # Auto-merge conditions:
          # 1. Patch updates for all dependencies
          # 2. Minor updates for devDependencies only
          UPDATE_TYPE="${{ steps.metadata.outputs.update-type }}"
          DEPENDENCY_TYPE="${{ steps.metadata.outputs.dependency-type }}"
          
          if [[ "$UPDATE_TYPE" == "version-update:semver-patch" ]]; then
            echo "should-merge=true" >> $GITHUB_OUTPUT
          elif [[ "$UPDATE_TYPE" == "version-update:semver-minor" && "$DEPENDENCY_TYPE" == "direct:development" ]]; then
            echo "should-merge=true" >> $GITHUB_OUTPUT
          else
            echo "should-merge=false" >> $GITHUB_OUTPUT
          fi

      - name: Enable auto-merge for Dependabot PRs
        # All status checks must pass before merge
        if: steps.should-auto-merge.outputs.should-merge == 'true'
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event_name == 'workflow_dispatch' && steps.pr-details.outputs.pr-url || github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Approve Dependabot PR
        # Approve the PR to help meet approval requirements if needed
        # Note: This auto-approves safe updates (patches and dev-only minors)
        # For production dependencies or major updates, manual review is still required
        if: steps.should-auto-merge.outputs.should-merge == 'true'
        run: gh pr review --approve "$PR_URL"
        env:
          PR_URL: ${{ github.event_name == 'workflow_dispatch' && steps.pr-details.outputs.pr-url || github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Comment on PR for major updates
        # Add a comment for major updates that won't be auto-merged
        if: steps.metadata.outputs.update-type == 'version-update:semver-major'
        run: |
          gh pr comment "$PR_URL" --body "🚨 **Major version update detected.** This PR requires manual review before merging to ensure compatibility."
        env:
          PR_URL: ${{ github.event_name == 'workflow_dispatch' && steps.pr-details.outputs.pr-url || github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
